module Chapter3.Practice3 where

{-
  second :: [a] -> a
  swap :: (b, a) -> (a, b)
  pair :: a -> b -> (a, b)
  double :: Num a => a -> a
  palindrome :: Eq a => [a] -> Bool
  twice :: (t -> t) -> t -> t
-}
